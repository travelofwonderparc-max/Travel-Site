<%- include('partials/header') %>

<div class="container admin-panel" style="max-width: 1400px; padding: 50px 20px;">
    
    <div class="admin-header" style="margin-bottom: 40px;">
        <h1 style="border-bottom: 3px solid var(--secondary); display: inline-block; padding-bottom: 10px;">🔧 Cockpit Administrateur</h1>
        
        <div style="display: flex; gap: 10px; flex-wrap: wrap; margin-top: 20px;">
            <a href="/admin/add-trip" class="btn-login" style="background: var(--secondary);">+ Ajouter un Séjour</a>
            <a href="/admin/import" class="btn-login" style="background: #6c757d;">📂 Importer Dossier</a>
            <a href="/admin/departs" class="btn-login" style="background: #e67e22;">✈️ Gérer les Départs</a>
            <a href="/admin/blog" class="btn-login" style="background: #9b59b6;">📰 Gérer le Blog</a>
            <a href="/admin/users" class="btn-login" style="background: #1abc9c;">👥 Annuaire Clients</a>
        </div>
    </div>

    <% if(success_msg && success_msg != '') { %>
        <div class="alert alert-success" style="background: #d4edda; color: #155724; padding: 15px; margin-bottom: 20px; border-radius: 5px;">
            ✅ <%= success_msg %>
        </div>
    <% } %>

    <div style="display: flex; gap: 20px; margin-bottom: 50px; flex-wrap: wrap;">
        
        <div style="flex: 1; min-width: 250px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 25px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1);">
            <div style="font-size: 1rem; opacity: 0.9;">Vues Page Accueil</div>
            <div style="font-size: 3rem; font-weight: bold;"><%= totalVisits %></div>
            <div style="font-size: 0.9rem; opacity: 0.8;">👀 Visiteurs uniques</div>
        </div>

        <div style="flex: 1; min-width: 250px; background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 99%, #fecfef 100%); color: #555; padding: 25px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1);">
            <div style="font-size: 1rem; opacity: 0.8;">Clients Inscrits</div>
            <div style="font-size: 3rem; font-weight: bold; color: #333;"><%= userCount %></div>
            <div style="font-size: 0.9rem;">👤 Comptes créés</div>
        </div>

        <div style="flex: 1; min-width: 250px; background: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%); color: #004d40; padding: 25px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1);">
            <div style="font-size: 1rem; opacity: 0.8;">Chiffre d'Affaires</div>
            <div style="font-size: 3rem; font-weight: bold;"><%= totalRevenue.toLocaleString() %> €</div>
            <div style="font-size: 0.9rem;">💰 Validé & Payé</div>
        </div>
    </div>

    <% if (pendingPros && pendingPros.length > 0) { %>
        <div class="admin-section" style="margin-bottom: 50px; background: #fff3cd; padding: 20px; border-radius: 10px; border: 1px solid #ffeeba;">
            <h2 style="color: #856404; margin-top: 0;">⏳ Pros en attente de validation (<%= pendingPros.length %>)</h2>
            <table class="admin-table" style="width: 100%; background: white;">
                <thead><tr><th>Société</th><th>Nom</th><th>Action</th></tr></thead>
                <tbody>
                    <% pendingPros.forEach(user => { %>
                        <tr>
                            <td><%= user.company %> (<%= user.siret %>)</td>
                            <td><%= user.name %></td>
                            <td><a href="/admin/validate/<%= user._id %>" class="btn-login" style="background: #28a745; font-size: 0.8rem;">Valider</a></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    <% } %>

    <div class="admin-section">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
            <h2>💳 Gestion des Réservations (<%= orders.length %>)</h2>
        </div>
        
        <div style="overflow-x: auto; background: white; border-radius: 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.05); padding: 20px;">
            <table class="admin-table" style="width: 100%; border-collapse: collapse;">
                <thead style="background: #f8f9fa; border-bottom: 2px solid #ddd;">
                    <tr>
                        <th style="padding: 15px; text-align: left;">Client</th>
                        <th style="padding: 15px; text-align: left;">Voyage</th>
                        <th style="padding: 15px; text-align: left;">Montant</th>
                        <th style="padding: 15px; text-align: left;">Statut</th>
                        <th style="padding: 15px; text-align: left;">Paiement</th>
                        <th style="padding: 15px; text-align: left;">Mise à jour</th>
                    </tr>
                </thead>
                <tbody>
                    <% orders.forEach(order => { %>
                        <tr style="border-bottom: 1px solid #eee;">
                            <td style="padding: 15px;">
                                <strong><%= order.userName %></strong><br>
                                <small style="color: #888;"><%= order.userEmail %></small>
                            </td>
                            <td style="padding: 15px;">
                                <%= order.tripTitle %><br>
                                <small><%= order.nbPeople %> pers. | <%= order.reservationNumber %></small>
                            </td>
                            <td style="padding: 15px; font-weight: bold;"><%= order.totalPrice %>€</td>
                            <td style="padding: 15px;">
                                <% 
                                    let badgeColor = '#6c757d';
                                    if(order.status === 'Payée' || order.status === 'Validée') badgeColor = '#28a745';
                                    if(order.status === 'En attente') badgeColor = '#ffc107';
                                    if(order.status === 'Refusée') badgeColor = '#dc3545';
                                %>
                                <span style="background:<%= badgeColor %>; color:white; padding:5px 10px; border-radius:20px; font-size: 0.85rem;">
                                    <%= order.status %>
                                </span>
                            </td>
                            <td style="padding: 15px;">
                                <%= order.paymentMethod %> <br>
                                <small>(<%= order.paymentStatus %>)</small>
                            </td>
                            <td style="padding: 15px;">
                                <form action="/admin/paiement" method="POST" style="display: flex; flex-direction: column; gap: 5px;">
                                    <input type="hidden" name="orderId" value="<%= order._id %>">
                                    
                                    <select name="status" style="padding: 5px; border-radius: 5px; border: 1px solid #ddd;">
                                        <option value="En attente" <%= order.status=='En attente'?'selected':'' %>>En attente</option>
                                        <option value="Validée" <%= order.status=='Validée'?'selected':'' %>>Validée (Acompte)</option>
                                        <option value="Payée" <%= order.status=='Payée'?'selected':'' %>>Payée (Soldé)</option>
                                        <option value="Terminée" <%= order.status=='Terminée'?'selected':'' %>>Terminée</option>
                                        <option value="Refusée" <%= order.status=='Refusée'?'selected':'' %>>Refusée</option>
                                    </select>

                                    <button type="submit" style="background: #17a2b8; color: white; border: none; padding: 5px; border-radius: 5px; cursor: pointer; font-size: 0.8rem;">
                                        💾 Mettre à jour
                                    </button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>

    <% if(locals.tripsStats) { %>
    <div class="admin-section" style="margin-top: 50px;">
        <h2>📊 Popularité des pages</h2>
        <table class="admin-table">
            <thead><tr><th>Voyage</th><th>Vues</th></tr></thead>
            <tbody>
                <% tripsStats.forEach(trip => { %>
                    <tr>
                        <td><%= trip.titre %></td>
                        <td><%= trip.vues %> vues</td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
    <% } %>

</div>
<%- include('partials/footer') %>